{
  "name": "lionrock-walkthrough",
  "private": true,
  "version": "1.0.0",
  "description": "Lion Rock interactive scroll-scrub video walkthrough (static site, no build step)",
  "scripts": {
    "config": "node scripts/gen-config.js",
    "dev": "npm run config && npx --yes serve -l 5000 ."
  }
}
